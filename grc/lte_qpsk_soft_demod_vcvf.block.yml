# auto-generated by grc.converter

id: lte_qpsk_soft_demod_vcvf
label: QPSK soft demod
category: '[LTE]'

parameters:
-   id: vlen
    label: vector length
    dtype: int

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ vlen }

outputs:
-   domain: stream
    dtype: float
    vlen: ${ vlen*2 }

templates:
    imports: import gnuradio.lte as lte
    make: lte.qpsk_soft_demod_vcvf(${vlen}, "$id")

file_format: 1
