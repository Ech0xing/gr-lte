# auto-generated by grc.converter

id: lte_sss_tagger_cc
label: SSS Frame Tagger
category: '[LTE]'

parameters:
-   id: fftl
    label: FFT length
    dtype: int

inputs:
-   domain: stream
    dtype: complex
-   domain: message
    id: frame_start

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import gnuradio.lte as lte
    make: lte.sss_tagger_cc(${fftl}, "$id")

file_format: 1
