# auto-generated by grc.converter

id: lte_mimo_remove_cp
label: MIMO remove CP
category: '[LTE]'

parameters:
-   id: fftl
    label: FFT length
    dtype: int
-   id: rxant
    label: RX antennas
    dtype: int
    hide: part
-   id: key
    label: tag key value
    dtype: string

inputs:
-   domain: stream
    dtype: complex
    multiplicity: ${ rxant }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ fftl }
    multiplicity: ${ rxant }

templates:
    imports: import gnuradio.lte as lte
    make: lte.mimo_remove_cp(${fftl}, ${rxant}, ${key})

file_format: 1
