# auto-generated by grc.converter

id: lte_pcfich_unpack_vfm
label: CFI unpack
category: '[LTE]'

parameters:
-   id: key
    label: tag key value
    dtype: string

inputs:
-   domain: stream
    dtype: float
    vlen: 32

outputs:
-   domain: message
    id: CFI
    optional: true

templates:
    imports: import gnuradio.lte as lte
    make: lte.pcfich_unpack_vfm(${key}, "CFI", "$id")

file_format: 1
