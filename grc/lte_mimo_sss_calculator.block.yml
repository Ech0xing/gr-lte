# auto-generated by grc.converter

id: lte_mimo_sss_calculator
label: MIMO SSS calculator
category: '[LTE]'

parameters:
-   id: rxant
    label: RX antennas
    dtype: int

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ 72*rxant }
-   domain: message
    id: N_id_2

outputs:
-   domain: message
    id: frame_start
-   domain: message
    id: cell_id

templates:
    imports: import gnuradio.lte as lte
    make: lte.mimo_sss_calculator(${rxant})

file_format: 1
