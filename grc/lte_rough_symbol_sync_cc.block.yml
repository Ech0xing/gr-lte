# auto-generated by grc.converter

id: lte_rough_symbol_sync_cc
label: CP-based Rough Symbol Sync Vec
category: '[LTE]'

parameters:
-   id: fftl
    label: FFT length
    dtype: int
-   id: vlen
    label: Vec length
    dtype: int

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ vlen }

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ vlen }

templates:
    imports: import gnuradio.lte as lte
    make: lte.rough_symbol_sync_cc(${fftl}, ${vlen}, "$id")

file_format: 1
